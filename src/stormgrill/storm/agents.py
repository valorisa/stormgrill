"""Five configurable expert agents: Practitioner, Academic, Skeptic, Economist, Historian.

Each agent researches the topic independently and in parallel (V0), blind to
the decision-maker's chat context (docs/protocol.md, Branch A).
"""

# TODO(V0): base Agent class + the 5 default expert profiles.
# TODO(V0): parallel execution with per-agent timeout/throttling.
