"""Transparency layer: context-leak audit and pivot log.

Ensures the Grill is never contaminated by unfiltered Storm reasoning
(docs/protocol.md, Branch A) and timestamps every framing change.
"""

# TODO(V1): context-leak detection between Storm and Grill.
# TODO(V1): pivot log with timestamps.
